<UserControl x:Class="ITHS_DB_Labb03.View.EditListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ITHS_DB_Labb03.View"
             xmlns:vm="clr-namespace:ITHS_DB_Labb03.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="100"
             d:DesignWidth="200"
             MinWidth="200"
             MinHeight="100"
             Background="White"
             BorderBrush="Black"
             BorderThickness="2">
    <StackPanel Orientation="Vertical"
                VerticalAlignment="Center"
                Margin="15 10">
        <TextBox Text="{Binding TodoCollectionViewModel.CurrentTodoCollection.Title}" />
        <Button Content="Save"
                Margin="0 10 "
                Command="{Binding TodoCollectionViewModel.UpdateListCMD}"/>
    </StackPanel>
</UserControl>
